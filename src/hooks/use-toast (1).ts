// This file existed with a duplicate name (likely created by the OS/editor).
// Keep a small re-export wrapper so existing references to the misnamed file
// continue to work while the canonical `use-toast.ts` is used.

export { useToast, toast } from './use-toast';
