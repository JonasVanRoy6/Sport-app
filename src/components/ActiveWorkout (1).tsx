// This file existed with a duplicate name (likely created by the OS/editor).
// Keep a tiny re-export wrapper so existing references to the misnamed file
// continue to work while the canonical `ActiveWorkout.tsx` is used.

export { ActiveWorkout } from './ActiveWorkout';
